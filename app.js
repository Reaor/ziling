/**
 * 应用层：把字灵接入页面 —— 交互、觅食、UI 喂食
 * 基于 virtualpet 原型 build 3.35.29
 */
(function () { "use strict"; if (!window.ZiLing || typeof window.ZiLing.Pet !== "function") { console.error("[ZiLing] Pet not defined"); return; } console.log("[ZiLing] app.js placeholder — full code in local git, push when network ok"); })();
