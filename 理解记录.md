# 字灵核心理解记录

> 此文档是实施前的理解锚点。所有内容从 `字灵最新收束.md` 和 `过程质询.md` 推导而来。
> 后续实施过程中若出现偏差，以本文档为判断依据。

---

## 一、字灵是什么

**所有可见物都是由同样大小的汉字（里字）拼成的马赛克。**

- 一个里字 = 一个固定字号的汉字，占据格子网上的一个单元格
- 不存在"大字"或"小字"——原态和动态的字大小完全一样
- 不存在"单个大号渲染的巨字"——巨字是几十个里字沿着笔画轮廓排列出来的

---

## 二、三模态定义

### 互动态（默认）
- 进入页面即呈现
- 日程信息传给AI → AI返回反馈内容 → 动态颜文字 → 回归原态
- 再次交互 → 各种形状：花、风车、爱心、时钟、数学曲线
- 用户行为：
  - 点击 → 被点处里字散开
  - 双击 → 切换当前形状
  - 拖拽 → 壳心跟手，里字沿格子追过去（非整体平移）
  - 长按 → 回归原态
  - 多次急点 → 负面情绪颜文字
  - 长时间不点 → 自动切换形状

### 对话态
- 左下角按钮展开输入框进入；收起输入框退出
- 进入时 → 随机颜文字形状
- 用户输入 → 发给AI → AI延迟回复（期间颜文字更活跃）

**完整对话回合流转（同一批里字贯穿全程）：**

```
用户发送文字
  ↓
[等待期] 当前颜文字活跃运动
  ↓
AI返回回复（三部分结构）
  ↓
第一部分：简洁回复句
  → 当前动态里字内容更新为AI回复的字
  → 聚合成原态（文本行排列）
  ↓
用户交互（点/拖/双击）触发进入第二部分
长按则回归原态
  ↓
第二部分：巨字形态呈现
  → AI指定：什么巨字、横排竖排、多字轮番时间、持续时长
  → 里字沿格子走到巨字轮廓目标位置
  → 持续时间结束 → 进入第三部分
  → 双击 → 直接进入第三部分
  ↓
第三部分：回复流（循环输出）
  每小段 = 一句回复 + 一个颜文字表情：
    原态呈现 → 聚合成动态颜文字 → 新原态 → 新颜文字 → ...
  用户交互：
    点击 → 散开
    拖拽 → 拖动
    长按 → 回归原态
    双击 → 打断当前循环，AI从打断处重新思考并输出新回复流
  ↓
用户新输入 → 打断当前状态 → 新一轮对话（有记忆）
收起对话框 → AI继续呈现一会儿 → 自动切回互动态
```

### 游戏态
- 左上角挂件按钮进入；右上角退出按钮退出（保存进度）
- 正方形格子网，每格一个字
- 消消乐规则：点击激活一字 → 再点可组词的字 → 两个消除
- 组词判定：AI兜底（词库难穷举）
- 计分 + 补字 + 墨色溢出特效 + 音效

---

## 三、运动系统（三层叠加）

同一批里字，三层运动叠加决定每帧最终位置：

### 第一层：随机游走（始终运行）
- 底层引擎，每个里字独立
- 格子上走：只能横向或纵向，不能斜
- 随机选方向，一个方向上走足够多格再换方向（避免一格一换的抽搐感）
- 不碰撞：不能走进别的字占着的格子
- 速率：所有里字匀速一致

### 第二层：形状约束（形成形状时叠加）
- 形状轮廓定义了"允许活动的格子集"（掩码）
- 颜文字/巨字 → 强约束：轮廓窄，允许格子少，里字运动更有序规整
- 数学曲线/花/爱心 → 弱约束：允许区域宽松，里字运动更自由
- 里字到达目标位置后，仍在允许区域内继续随机游走

### 第三层：微动生命（颜文字模式叠加）
- 在形状约束基础上，局部区域小幅有节奏偏移
- 挤眼睛、弯眼睛、撇嘴 → 对应区域的里字目标位置做周期性 easing
- 间距呼吸感 → 全局间距做微小缩放波动

---

## 四、原态 ↔ 动态切换机制

**核心：始终是同一批里字，改变的是排列方式。不销毁重建。**

### 动态 → 原态
1. 当前形状里的里字 → 各自沿格子规划路径
2. 走向文本行的目标位置（按行排布，行间距 = 格子间距）
3. 到达后形成整齐的文本行
4. 过程中可能伴随内容更新（AI回复的字替换原来字的内容）

### 原态 → 动态
1. 计算目标形状的格子掩码 → 分配目标位置给每个里字
2. 里字从文本行位置 → 沿格子走到形状中的目标位置
3. 自适应增减：目标位置多 → 边缘优雅浮现新里字；目标位置少 → 多余里字优雅淡出

### 对话态流程特化
- AI返回新内容 → 当前动态里字**内容更新** → 再聚合成原态
- 不是：销毁旧的 → 生成新的 → 排成原态
- 是：**同一批里字，字符内容变了，排列方式变了，走到新位置**
- **"逐个消失→浮现"是技术妥协，非设计意图。实现了MAPF后可直接内容更新+走位过渡。**

---

## 五、关键技术本质

1. **多智能体路径规划（MAPF）**：100-300个智能体在离散格子上并发寻路，不碰撞
2. **形状采样 → 格点映射**：离屏Canvas渲染目标形状 → getImageData提取像素 → 映射到格点坐标 → 生成"允许格子集"掩码
3. **里字生命周期**：自适应增减——优雅浮现/淡出，不突然出现或消失
4. **格子内插值动画**：即使逻辑上是离散格子移动，视觉上需要子像素平滑过渡

---

## 六、技术选型结论

**Canvas 2D + Vanilla JS + WebView嵌入**

原因：
- 渲染需求极简：fillText(x, y, char) 就够
- 核心难点是算法（MAPF、形状采样），不是渲染
- 离屏Canvas做形状采样天然顺手
- 零依赖，直接 file:// 加载嵌入移动端WebView
- 任何框架都解决不了MAPF问题，只会增加抽象层

---

## 七、实施进度（2025-05-27）

### 已完成

| 阶段 | 内容 | 文件 | 验证 |
|---|---|---|---|
| Phase 0 | 项目骨架 | index.html, app.css, renderer.js | 390x700, DPR=2, 60fps |
| Phase 1 | 格子系统 + 里字模型 | grid.js, character.js | 数据操作正确 |
| Phase 2-4 | 运动引擎（经过多次迭代） | motion.js, app.js | — |
| **PIBT 重写** | **基于 PIBT 算法彻底重写运动引擎** | **motion.js (370行)** | **100字/30s/零碰撞/60fps** |

### PIBT 引擎核心参数（2025-05-27 最终）
- 算法：PIBT + 排斥力 + 目标方向优先级
- 里字数：60
- 格子：16px cell / 15px 字体 / 24×43
- 速度：200ms/tick（统一匀速）
- 碰撞：零（数学保证）
- 帧率：60 FPS 稳定
- 排序策略：
  - **有目标时**：目标方向最高优先
  - **无目标时**：方向持续性 + 禁 180° 反弹 + 15-29 tick 随机 90° 拐弯
- 游荡：目标必选 8 格外
- 初始：随机方向 + 随机 streak（防同向潮）
- 插值：线性 + 无 stagger + 每字独立 moveStart
- 卡死：5 tick 后远距目标重分配 / 120 tick 后稀疏区重生

### 形状系统（初步验证）
- `shape.js`：15 颜文字模板 + 巨字 + 曲线离屏采样
- **矩形形状测试**：12×5 = 60 格点 → 60 字成功短暂聚拢 ✓
- **目标优先级修复**：形状模式下目标方向为最高优先（之前被方向持续性压过）✓
- 形状约束：`constrainToShape()` + `shapeMask`，到位后在掩码内继续游走
- 待验证：颜文字 ^_^ 等复杂形状的实际可辨认性
- 已知限制：100% 密度时（N字=N格掩码）全部堵住 → 需降低密度或减少约束字

### 已有的文件（2025-05-27）
```
index.html                    入口
css/app.css                   移动端样式
js/app.js                     主控（创建字、形状触发、渲染、FPS）
js/core/grid.js               格子系统
js/core/character.js          里字模型 + 对象池
js/core/motion.js             PIBT 运动引擎（~420行）
js/core/shape.js              形状采样系统
js/render/renderer.js         Canvas 渲染器
```

### 待实施
- Phase 7-9：拖拽 + 点击散开 + 三层运动叠加
- Phase 10-14：手势识别 + 互动态/对话态状态机 + 文本行布局
- Phase 15-21：游戏态、原态动画、AI 接口、设置系统
